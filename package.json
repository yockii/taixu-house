{
  "name": "taixu-house",
  "version": "0.1.0",
  "description": "太虚数字生命官方示例 UI —— 像素小屋。同时是对接 runtime Life SDK 的参考实现与上手教程。",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
